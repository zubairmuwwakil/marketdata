name: CI

on:
  push:
    branches: [ "main", "render_2" ]
  pull_request:
    branches: [ "main", "render_2" ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build, test, and run deploy smoke
        run: ./mvnw -B verify
